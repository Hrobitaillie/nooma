# Zone de rate-limit des écritures de l'atelier Plouma (doc 18 §3.1).
# À déposer dans /etc/nginx/conf.d/plouma_limit.conf (niveau http), puis nginx -t && reload.
limit_req_zone $binary_remote_addr zone=plouma_ecriture:1m rate=30r/m;
